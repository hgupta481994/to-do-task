<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
* {
    box-sizing: border-box;
}

thead,tbody { display: block; }

tbody {
    height: 400px;       
    overflow-y: auto;    /* Trigger vertical scroll    */
   overflow-x:auto ; 
}

table {
	
    border-collapse: collapse;
    border-spacing: 0;
    width: 100%;
    border: 1px solid #ddd;
}

th, td {
    text-align: left;
    padding: 8px;
}

body {
    margin: 0;
}

/* Create two equal columns that floats next to each other */
.column {
    float: left;
    width: 50%;
    padding: 10px;
    height: 500px; 
}

/* Clear floats after the columns */
.row:after {
    content: "";
    display: table;
    clear: both;
}
</style>
</head>
<body>

<h1>Team lead section</h1>
<br>
<div class="row">
	<div class="column" style="background-color:#fff;width:30%;">
		<h2>Team Leads assigned</h2>
		<table >
			<tr>
		      <th>Team Lead</th>
		    </tr>
			<% for t in @tleads %>
				<tr>
					<% if (t.username != 'admin') %>
						<td><%= t.username %></td>
						<td><%= link_to 'Assign users to TL', assign_user_path(t) %>
					<% end %>
				</tr>
			<% end %>
		</table>
	</div>
	<div class="column" style="background-color:#fff;width:50%;">
		<h2>Assign team lead to employees</h2>
			<table >
				<tr>
			      <th>Users</th>
			      <th></th>
			    </tr>
				<% for u in @users %>
					<tr>
						<% if u.is_tlead? == false %>
							<td><%= u.username %> </td>
							<td><%= link_to " Make Team Lead ", tlead_multiple_path(u), method: :patch %>
					 	<% end %>
					</tr>
				<% end %>
		</table>
	</div>

	
